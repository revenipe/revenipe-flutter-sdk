name: revenipe_flutter
description: Billing, subscriptions, trials and entitlements for Flutter apps powered by Revenipe.
version: 0.1.5
homepage: https://revenipe.com
repository: https://github.com/revenipe/revenipe-flutter-sdk
documentation: https://docs.revenipe.com
issue_tracker: https://github.com/revenipe/revenipe-flutter-sdk/issues

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"


screenshots:
  - description: 'Configure products, connect the Revenipe Flutter SDK, and start collecting payments.'
    path: assets/readme/go_live_in_3_steps.png

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
